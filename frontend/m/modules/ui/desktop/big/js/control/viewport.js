/**
 * @fileOverview Layout of application
 */
C.utils.inherit('O.view.Viewport', {
/**
	* Updates connection status icon
	* @param Boolean connection
	*/
	updateConnectionStatus: function(connection) {
		//this.headContainer.updateConnectionStatus(connection);
	}
});