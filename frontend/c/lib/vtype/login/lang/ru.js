/**
 *
 * RU
 * 
 */
/*
C.onload(function() {
	if (C.utils.isset('Ext.form.field.VTypes')) {
		Ext.apply(Ext.form.field.VTypes, {
			uidText: _('Already exists or less than 8 characters')
		});
	}
});

_({
	'Already exists or less than 8 characters':
		'Уже существует'
});*/