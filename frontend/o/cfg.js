/**
 * Configuration options
 * @class C.cfg
 * @singleton
 */
C.cfg.events = {
/**
	* Maximum count of message on the screen.
	* If count of message is more than this value,
	* they are packed into message container.
	* @cfg maxNumber
	*/
	maxNumber: 5
};
